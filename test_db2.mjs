import 'dotenv/config'; import { PrismaClient } from '@prisma/client'; import { PrismaNeonHttp } from '@prisma/adapter-neon'; const connectionString = process.env.DATABASE_URL; const adapter = new PrismaNeonHttp(connectionString, { fetchOptions: {} }); const prisma = new PrismaClient({ adapter }); async function main() { try { const orders = await prisma.order.findMany(); console.log('success', orders.length); } catch (e) { console.error('Error:', e); } } main();
